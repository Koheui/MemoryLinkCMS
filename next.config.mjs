/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // reactStrictMode: false, // Disabling strict mode can sometimes help with obscure hydration errors during development, but use with caution.
};

export default nextConfig;
